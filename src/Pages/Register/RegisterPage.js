import React from "react";
import { Form, Navbar, Footer } from "../../components";
const RegisterPage = () => {
	return (
		<div>
			<Navbar />
			<Form />
			<Footer />
		</div>
	);
};

export default RegisterPage;
